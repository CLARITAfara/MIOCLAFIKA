package com.mixeo.api.repository;

import com.mixeo.api.model.PlaylistRule;
import org.springframework.data.jpa.repository.JpaRepository;

public interface PlaylistRuleRepository extends JpaRepository<PlaylistRule, Integer> {
}
